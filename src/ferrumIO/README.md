# ferrumIO

Planned native `FerrumFile` parsing, writing, schema validation, restart data,
and result I/O. External-format compatibility remains an isolated adapter
concern and does not define native Ferrum semantics.
