# Validation Scripts

These scripts reproduce external references and development validation. They
are not end-user commands. Stable user workflows are exposed as Rust binaries.

See `docs/development/script-policy.md` for retention and cleanup rules.
