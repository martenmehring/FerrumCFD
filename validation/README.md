# Validation

Cross-case validation logic, common acceptance schemas, and stable reference
snapshots belong here. Runnable source cases remain under `tutorials/`, while
generated meshes, fields, logs, JSON, and Markdown reports belong under
`target/`.

Every accepted application driver must satisfy the common readiness gate in
`docs/solver-roadmap.md`.
