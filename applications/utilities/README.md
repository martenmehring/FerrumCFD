# Utilities

Mesh conversion, case setup, validation, and post-processing executables will
move here from `../legacy/ferrumCli` as their native `FerrumFile v1` contracts
stabilize. The `mesh/`, `case/`, and `postProcessing/` directories define the
permanent responsibility boundaries. Public command names remain unchanged
during that split.
